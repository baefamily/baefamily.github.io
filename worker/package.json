{
  "name": "baefamily-api",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "latest"
  },
  "dependencies": {
    "@block65/webcrypto-web-push": "^1.0.2"
  }
}
